# 安全性文档

本文档描述了 Warden Worker 项目的安全功能和最佳实践。

## 已实施的安全措施

### 1. 密码学安全

- **PBKDF2-SHA256 密钥派生**: 使用 PBKDF2-SHA256 进行密钥派生，默认迭代次数为 600,000 次
- **KDF 迭代次数验证**: 强制要求 KDF 迭代次数在 100,000 到 2,000,000 之间
- **恒定时间比较**: 使用 `constant_time_eq` 库进行密码哈希比较，防止时序攻击
- **安全令牌生成**: 使用 UUID v4 生成安全戳和其他随机标识符

### 2. 认证和授权

- **JWT 令牌认证**: 使用 JSON Web Tokens 进行用户认证
- **访问令牌**: 1 小时有效期
- **刷新令牌**: 30 天有效期
- **安全戳 (Security Stamp)**: 每次密码更改时生成新的安全戳，使旧令牌失效
- **授权检查**: 所有数据访问都验证用户 ID，确保用户只能访问自己的数据

### 3. 数据保护

- **参数化查询**: 所有数据库查询使用参数化语句，防止 SQL 注入
- **软删除**: 密码项目使用软删除机制（`deleted_at` 字段），可以恢复
- **外键约束**: 数据库使用外键约束和级联删除，确保数据完整性
- **数据加密**: 所有敏感数据（密码、笔记等）在客户端加密后存储

### 4. 输入验证

- **邮箱格式验证**: 验证邮箱地址格式的基本有效性
- **KDF 参数验证**: 验证 KDF 类型和迭代次数
- **非空检查**: 验证密码哈希、加密密钥等关键字段不为空
- **邮箱白名单**: 通过 `ALLOWED_EMAILS` 环境变量限制注册

### 5. CORS 配置

- **跨域资源共享**: 配置了 CORS 以允许 Bitwarden 客户端访问
- **灵活的源策略**: 允许所有源访问（适用于公共 API）

### 6. 会话管理

- **令牌刷新**: 支持使用刷新令牌获取新的访问令牌
- **令牌过期**: 访问令牌自动过期，减少令牌泄露风险
- **密码更改后失效**: 更改密码会生成新的安全戳

## 安全最佳实践

### 部署时的建议

1. **设置强密钥**: 为 `JWT_SECRET` 和 `JWT_REFRESH_SECRET` 环境变量设置长的随机字符串
2. **限制注册**: 使用 `ALLOWED_EMAILS` 环境变量限制可以注册的邮箱地址
3. **启用 HTTPS**: 确保您的 Cloudflare Worker 通过 HTTPS 提供服务
4. **定期备份**: 定期备份 D1 数据库数据
5. **监控日志**: 监控应用程序日志以发现异常活动

### 用户建议

1. **使用强主密码**: 选择一个强大且唯一的主密码
2. **保管好主密码**: 主密码一旦丢失无法恢复
3. **定期更改密码**: 建议定期更改主密码
4. **启用两因素认证**: （待实现）未来版本将支持两因素认证

## 已知限制

### 当前未实现的功能

- **速率限制**: 目前没有实施 API 速率限制，可能容易受到暴力攻击
- **两因素认证**: 尚未实现 2FA
- **邮箱验证**: 邮箱验证功能仅为模拟实现
- **IP 封禁**: 没有 IP 封禁或异常检测机制
- **审计日志**: 没有详细的安全审计日志
- **附件支持**: 不支持附件上传（避免了相关的安全风险）

### 依赖项安全

项目依赖以下关键安全库：

- `constant_time_eq`: 防止时序攻击
- `jsonwebtoken`: JWT 令牌处理
- `chrono`: 时间戳处理
- `uuid`: 安全随机标识符生成

定期使用 `cargo audit` 检查依赖项中的已知漏洞。

## 报告安全问题

如果您发现安全漏洞，请不要公开披露。请通过以下方式联系维护者：

1. 在 GitHub 上创建私密的安全咨询
2. 或通过项目维护者的邮箱联系

## 更新日志

- **2025-12-28**: 
  - 添加输入验证
  - 实现软删除功能
  - 添加密码更改功能
  - 添加账户删除功能
  - 改进错误处理
  - 添加安全文档
